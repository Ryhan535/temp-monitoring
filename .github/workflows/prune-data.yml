name: Prune Supabase

on:
  schedule:
    - cron: "0 0 * * *"   # 1x sehari (jam 00:00 UTC)
  workflow_dispatch:

jobs:
  prune:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run prune script
        run: firebase-bridge/php prune-data.php
